defaults:
  - _self_
  - override hydra/launcher: ../../../../hydra/launcher/cluster_4G

parsing_fun: experiments.src.memory_requirements.destriping_only

experiment_output_dir: results/memory_requirements/destriping/zebrafish_head

benchmark_output_file: "experiments/benchmark_output_files/memory_requirements/zebrafish_head.yaml"

dataset:
  path_data: "results/Visium_HD_Zebrafish_Head_tissue/10x_segmentation__09_01_26__14_51_29"
  cell_id_label: "cell_id"

hydra:
  job:
    chdir: true
  sweep:
    dir: ${experiment_output_dir}/${now:%Y-%m-%d}/${now:%H-%M-%S}
