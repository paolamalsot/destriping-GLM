defaults:
  - _self_
  - override hydra/launcher: ../../../../hydra/launcher/cluster_4G

parsing_fun: experiments.src.memory_requirements.destriping_only

experiment_output_dir: results/memory_requirements/destriping/mouse_brain

benchmark_output_file: "experiments/benchmark_output_files/memory_requirements/mouse_brain.yaml"

dataset:
  path_data: "results/Visium_HD_Mouse_Brain_tissue/stardist_segmentation__24_02_25__18_08_11/sdata"
  cell_id_label: "labels_he"

hydra:
  job:
    chdir: true
  sweep:
    dir: ${experiment_output_dir}/${now:%Y-%m-%d}/${now:%H-%M-%S}
