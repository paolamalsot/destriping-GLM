defaults:
  - _self_
  - override hydra/launcher: ../../../../hydra/launcher/cluster_8G

parsing_fun: experiments.src.memory_requirements.destriping_only

experiment_output_dir: results/memory_requirements/destriping/mouse_embryo

benchmark_output_file: "experiments/benchmark_output_files/memory_requirements/mouse_embryo.yaml"

dataset:
  path_data: "results/Visium_HD_Mouse_Embryo_tissue/10x_segmentation__09_01_26__10_51_37"
  cell_id_label: "cell_id"

hydra:
  job:
    chdir: true
  sweep:
    dir: ${experiment_output_dir}/${now:%Y-%m-%d}/${now:%H-%M-%S}
